<resources>

    <!-- Style pour les EditText classiques (version Classic) -->
    <style name="StyledInput">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">@drawable/rounded_input_background</item>
        <item name="android:padding">12dp</item>
        <item name="android:textColor">#000000</item>
        <item name="android:textSize">16sp</item>
        <item name="android:layout_marginBottom">16dp</item>
    </style>

    <!-- Style pour les MaterialButton -->
    <style name="Widget.MyMaterialButton" parent="Widget.MaterialComponents.Button">
        <item name="cornerRadius">16dp</item>
        <item name="android:elevation">6dp</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- Style pour MaterialCardView -->
    <style name="Widget.MyMaterialCard" parent="Widget.MaterialComponents.CardView">
        <item name="cardCornerRadius">20dp</item>
        <item name="cardElevation">8dp</item>
        <item name="strokeColor">@color/button_primary</item>
        <item name="strokeWidth">1dp</item>
    </style>

</resources>
